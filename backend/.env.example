# Backend server
PORT=5000
NODE_ENV=production
CORS_ORIGIN=http://localhost:3000

# PostgreSQL (service name "postgres" from docker-compose network)
DB_HOST=postgres
DB_PORT=5432
DB_USER=shopeasy_user
DB_PASSWORD=change_me_strong_password
DB_NAME=shopeasy

# Redis (service name "redis" from docker-compose network)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=change_me_redis_password
