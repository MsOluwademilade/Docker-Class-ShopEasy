# Backend server
PORT=5000
NODE_ENV=production
CORS_ORIGIN=http://localhost:3000

# PostgreSQL (service name "postgres" from docker-compose network)
DB_HOST=postgres
DB_PORT=5432
DB_USER=shopeasy_demilade
DB_PASSWORD=my_strong_postgres_password
DB_NAME=shopeasy

# Redis (service name "redis" from docker-compose network)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=my_strong_redis_password
