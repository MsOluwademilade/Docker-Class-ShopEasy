# Only used by `npm run dev` (local Vite dev server) to know where to proxy
# /api requests. NOT used in the Docker image - nginx.conf handles that via
# the Compose network, so nothing is baked in at build time.
VITE_DEV_API_TARGET=http://localhost:5000
